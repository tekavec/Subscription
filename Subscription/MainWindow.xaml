<Window x:Class="Subscription.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Subscription"
        mc:Ignorable="d"
        Title="Subscription" Height="450" Width="800" WindowState="Maximized">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0">
            <ComboBox  x:Name="yearsComboBox" ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Margin="12">
            </ComboBox>
            <ListBox x:Name="monthsListBox" ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" SelectionMode="Single" Margin = "12" />
            <Button x:Name="createNewMonthlySubscriptionButton" Margin="12">Create new</Button>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <DataGrid Grid.Column="1" x:Name="dataGrid1" 
                  Margin="12"
                  AutoGenerateColumns="True">
        </DataGrid>
        </StackPanel>
    </Grid>
</Window>
